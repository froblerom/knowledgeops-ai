# Implementation Decisions Log

This log tracks implementation-time and harness-routing decisions. Accepted ADRs remain authoritative for architecture decisions.

| Date | Decision | Rationale | Affected Area | ADR Needed |
| --- | --- | --- | --- | --- |
| 2026-05-24 | Use `docs/agents/` as the canonical agent context harness root; do not use `docs/agent/`. | Multiple shared contexts, specialist definitions and progress files require one unambiguous plural modular location. | Agent harness | No |
| 2026-05-24 | Use prompt Levels 0 through 3 only. | Level 3 covers cross-layer, security-sensitive, harness and release work for current MVP. | Prompt routing | No |
| 2026-05-24 | Add dedicated `rag-implementation-agent`. | RAG safety crosses authorization, prompts, citations, provider isolation and sensitive AI telemetry. | Agent harness | No |
| 2026-05-24 | Use progress files before implementation prompts begin. | Prompts require current state, decisions, risks and verified completion history. | Execution workflow | No |
| 2026-05-24 | Mermaid Markdown remains diagram source of truth. | Accepted ADR-009; rendered images remain artifacts. | Documentation/diagrams | No |
| 2026-05-24 | Angular is selected for MVP frontend. | Accepted ADR-003. | Frontend | No |
| 2026-05-24 | `docs/09-business-rules.md` is canonical for `BR-###`. | Prevent conflicting rule identifiers and traceability drift. | Rules/traceability | No |
| 2026-05-24 | Document disablement uses `is_retrieval_enabled = false`, not a `Disabled` processing status. | Keeps processing outcome separate from retrieval availability. | Documents/retrieval | No |
| 2026-05-24 | Future implementation prompts must classify first using `docs/agents/13-prompt-classifier.md`. | Ensures scope, required context, risk and validation expectations are declared before implementation begins. | Execution workflow | No |
| 2026-05-25 | Use `KnowledgeOps` for code projects and namespaces, with `KnowledgeOpsAI.sln` as the solution name while retaining KnowledgeOps-AI branding. | Matches the approved scaffold audit and keeps code identifiers consistent without changing product branding. | Backend solution structure | No |
| 2026-05-25 | Target `net10.0` and pin the installed .NET SDK `10.0.204` in `global.json` with .NET 10 minor roll-forward. | Establishes a reproducible backend foundation without downgrading the approved target framework. | Backend toolchain | No |
| 2026-05-25 | Defer `KnowledgeOps.E2ETests` beyond Issue #3. | Issue #3 requires only the four approved scaffold test projects; E2E workflow coverage belongs to a later authorized sprint. | Testing structure | No |
| 2026-05-25 | Keep Issue #3 dependencies limited to host/DI abstractions and xUnit template support. | Persistence, security, provider SDKs, observability integrations and container testing are outside scaffold scope. | Backend dependency surface | No |
| 2026-05-25 | Use Angular 21.2.12 with Vitest (not Karma) as test runner. | Angular 21 ships `@angular/build:unit-test` backed by Vitest + jsdom; Karma is not available. | Frontend toolchain | No |
| 2026-05-25 | Auth guard returns `true` unconditionally until Sprint 6. | Route protection is UX guidance only; backend authorization is source of truth. Sprint 6 implements real auth. | Frontend security | No |
| 2026-05-25 | API interceptor is pass-through only; no JWT or Authorization header injection. | Real auth headers deferred to Sprint 6; pass-through keeps the interceptor chain wired without premature JWT handling. | Frontend HTTP | No |
| 2026-05-25 | Development `apiBaseUrl` points to `http://localhost:5194/api/v1` (port from `launchSettings.json` HTTP profile). | Sprint 3 may change this when Docker Compose local environment is configured; port stored in environment file for easy update. | Frontend/API integration | No |
| 2026-05-25 | Angular 21 does not generate `src/environments/` by default; files created manually. | Angular 15+ removed auto-generation; `fileReplacements` in `angular.json` development configuration performs the swap. | Frontend environment config | No |
| 2026-05-25 | Use hybrid local runtime mode as primary: SQL Server via Docker Compose; API/Worker via `dotnet run`; frontend via Angular CLI. | Aligns with `docs/18-deployment-and-devops.md` Section 3.5 Option A; avoids app container complexity before persistence is implemented. | Local DevOps | No |
| 2026-05-25 | Use `mcr.microsoft.com/mssql/server:2022-latest` with `MSSQL_PID=Developer` for local SQL Server container. | SQL Server 2022 aligns with ADR-002; Developer edition is free for development and testing. | Local DevOps / Database | No |
| 2026-05-25 | Commit `.env.example` with safe placeholder values; ignore `.env` via `.gitignore`. | Matches `docs/18-deployment-and-devops.md` Section 11.2; safe template is committed, live secrets are never committed. | Security / Environment config | No |
| 2026-05-25 | Local document storage convention: `.local/storage/documents/` at repository root; ignored by `.gitignore`. | Keeps local runtime artifacts out of source control; provides a consistent path for future local storage adapter. | Local DevOps / Document storage | No |
| 2026-05-25 | Do not create API/Worker/Frontend Dockerfiles in Issue #5. | No persistence or application workflow is implemented yet; app containerization deferred until a later sprint when the full local stack can be validated end-to-end. | Local DevOps | No |
| 2026-05-25 | Use `KNOWLEDGEOPS_SQL_PORT` env var with default `1433` to allow port override if host port is already in use. | Prevents Docker Compose bind failure when a local SQL Server instance occupies port 1433. | Local DevOps | No |
| 2026-05-25 | Keep EF Core SQL Server and Design packages, `KnowledgeOpsDbContext`, configurations, migrations, and the design-time factory inside `KnowledgeOps.Infrastructure` only. | Preserves ADR-001/ADR-005 boundaries; Domain and Application remain persistence-framework independent. | Persistence architecture | No |
| 2026-05-25 | Issue #6 creates only `organizations`, `users`, `user_roles`, and `audit_log_entries`; role assignments store the five MVP `role_name` values and no `roles` table exists. | `docs/14-database-design.md` is canonical when issue fallback guidance differs; feature tables remain owned by future sprints. | Database foundation / scope | No |
| 2026-05-25 | Implement canonical nullable foreign keys from `audit_log_entries` to organizations and users, while leaving polymorphic `entity_id` unenforced. | Canonical database design lists optional organization/user relationships and explicitly avoids a polymorphic entity FK. | Audit persistence / organization scope | No |
| 2026-05-25 | Use a local `.config/dotnet-tools.json` manifest for `dotnet-ef` `10.0.8` and execute migration commands through `KnowledgeOps.Infrastructure` design-time startup. | Pins migration tooling while keeping `Microsoft.EntityFrameworkCore.Design` out of API/Application/Domain. | Tooling / persistence boundary | No |
| 2026-05-25 | Apply migrations intentionally during development/deployment validation; do not auto-run migrations from API or Worker startup. | Matches MVP deployment guidance and avoids unexpected schema changes during host startup. | Migration execution | No |
| 2026-05-25 | Use EF Core `HasData` with a separate `KnowledgeOpsSeedData` helper class and `SeedDataIds` constants for fictional seed data. | Keeps deterministic IDs and seed logic in one auditable location; `HasData` generates a proper `InsertData` migration that can be rolled back via `Down()`; avoids inline migration SQL. | Seed data / persistence | No |
| 2026-05-25 | Name fictional organizations "Asteria Support Group" and "Boreal Contact Services" with IANA-reserved `*.example.com` email domains. | Prevents any accidental delivery to real email addresses; fictional company names are clearly non-real. | Seed data / data safety | No |
| 2026-05-25 | Seed seven users (five in Asteria, two in Boreal) covering all five MVP roles; use deterministic GUIDs with memorable bit patterns (all-a, all-b, all-c prefixes). | Enables stable test references without dynamic lookup; two organizations support cross-org isolation tests; all five MVP roles are represented; Boreal Agent and Admin enable scope-boundary scenarios. | Seed data / testing | No |
| 2026-05-25 | Use fixed seed timestamp `2026-01-01T00:00:00Z`; never use `DateTimeOffset.UtcNow` in `HasData`. | `UtcNow` changes on every `dotnet ef migrations add` invocation and creates spurious diffs. Fixed timestamp produces a stable, idempotent migration. | Seed data / migration stability | No |
| 2026-05-25 | Store no passwords or password hashes in the seed migration. | Matches `docs/demo-data.md` safety rules; authentication credentials are out of scope for Sprint 5. Sprint 6 will introduce safe local credential setup. | Seed data / security | No |
| 2026-05-25 | `SeedDataTests.cs` integration tests use unique per-test database names (`KnowledgeOpsSeedDataTests_{Guid}`) and drop the database in `finally`. | Prevents seed test state from polluting or conflicting with the local `KnowledgeOpsLocal` development database. | Testing / data isolation | No |
| 2026-05-25 | Fixed `SqlServerPersistenceTests.cs` `SingleAsync()` fragility before adding seed data. | Helper methods assumed the table was empty; after the seed migration inserts 7 users and 7 roles, `SingleAsync()` would throw. Fix captures entity IDs from test-created objects and passes them as parameters. | Testing / correctness | No |

| 2026-05-25 | Use BCrypt.Net-Next 4.0.3 for password hashing (not Microsoft.AspNetCore.Identity). | `Microsoft.AspNetCore.Identity.Core` is tied to the ASP.NET Core release train; Infrastructure uses `Microsoft.NET.Sdk` (not Web) and version compatibility with net10.0 is uncertain. BCrypt.Net-Next targets netstandard2.0 and is version-agnostic. | Authentication / Infrastructure | No |
| 2026-05-25 | Use `System.IdentityModel.Tokens.Jwt 8.12.1` for JWT creation. | Widely used, well-maintained, and compatible with net10.0; avoids coupling to ASP.NET Core release cycle. | Authentication / Infrastructure | No |
| 2026-05-25 | JWT signing key must be at least 32 characters; validated via `IOptions<JwtSettings>.ValidateOnStart()`. | Fail-fast security contract from `docs/16-security-and-permissions.md`; `ValidateOnStart` defers validation to host startup so WebApplicationFactory tests can inject configuration first. | Authentication / Infrastructure | No |
| 2026-05-25 | Configure `JwtBearerOptions` via `AddOptions<JwtBearerOptions>().Configure<IOptions<JwtSettings>>(...)` (lazy, not inline). | Reading `Jwt:SigningKey` directly in `Program.cs` during DI registration fires before WebApplicationFactory can inject test configuration; the `IOptions`-based approach resolves the key lazily at first middleware use. | Authentication / API | No |
| 2026-05-25 | GET /auth/me re-queries the database by `userId`; never trusts JWT claims alone. | Security requirement: revoked or disabled accounts must be rejected even when a valid JWT is presented. | Authentication / API | No |
| 2026-05-25 | All login failures (unknown email, wrong password, Disabled, Pending, null hash) return HTTP 401 with an identical response body. | Prevents user enumeration; matches `docs/16-security-and-permissions.md` SEC-AUTH-001. | Authentication / API | No |
| 2026-05-25 | JWT logout is stateless (client-side token clear only); no server-side session table or token blacklist. | MVP scope constraint; no refresh tokens or session management in Sprint 6. | Authentication / API | No |
| 2026-05-25 | CORS restricted to `http://localhost:4200` in Development environment only. | Prevents cross-origin credential leakage in production; development-only allows Angular CLI dev server. | Authentication / API | No |
| 2026-05-25 | `AuthSessionService` wraps all `localStorage` access; no component writes storage directly. | Keeps storage concerns in one auditable service; enables easy test stubs. | Frontend / Authentication | No |
| 2026-05-25 | `apiInterceptor` attaches `Authorization: Bearer` only for requests whose URL starts with `environment.apiBaseUrl`. | Prevents token leakage to third-party URLs that may be requested from the same app. | Frontend / HTTP | No |
| 2026-05-25 | `WebApplicationFactory` tests for API layer use a `PlaintextPasswordHasher` stub that compares passwords directly. | BCrypt is slow by design; using it in API integration tests would make the test suite impractically slow. Application unit tests test hashing logic separately. | Testing / API | No |
| 2026-05-25 | SQL-gated auth integration tests (`AuthIntegrationTests`) use `AddInfrastructure` via a `ServiceCollection` and provision passwords via `IPasswordHasher` from DI; passwords are NOT stored in seed data. | Matches security contract: no password hashes in migrations or seed data; passwords provisioned in test setup only and discarded with the temporary test database. | Testing / Authentication / Security | No |

| 2026-05-26 | Place `IPermissionService`, `PermissionService`, `IOrganizationScopeService`, and `OrganizationScopeService` in `KnowledgeOps.Application`; no Infrastructure layer involvement. | Both services are pure in-memory (no database, no EF Core); placing them in Application keeps them testable without DbContext and maintains the clean Architecture boundary where permission semantics belong to Application, not Infrastructure. | Authorization architecture / layer placement | No |
| 2026-05-26 | Define `KnowledgeOpsPermissions` as nested static classes with `const string` Resource.Action constants and an `AllMvpPermissions` `IReadOnlySet<string>`. | Provides compile-time permission references, prevents string-literal drift, and gives `RolePermissionMatrix` and `RequirePermission` a shared canonical source without coupling to any framework type. | Permission catalog | No |
| 2026-05-26 | `Admin` role receives `new HashSet<string>(KnowledgeOpsPermissions.AllMvpPermissions, StringComparer.Ordinal)` — all 30 MVP permissions. | Admin is the org-level super-user with full access within the organization; granting `AllMvpPermissions` ensures Admin tests never need to be updated when new MVP permissions are added to the catalog. | Role-permission matrix | No |
| 2026-05-26 | `OrganizationScopeService.CheckScope` applies identically to all roles including Admin. | ADR-010: Admin is same-organization-only in MVP; no cross-organization super-admin model exists. Applying the scope check uniformly prevents inadvertent Admin bypass and simplifies the authorization formula. | Organization scope / Admin role | No |
| 2026-05-26 | Use `RequirePermissionAttribute : AuthorizeAttribute` with a `"Permission:"` policy prefix and `PermissionPolicyProvider` to map named policies. | Leverages ASP.NET Core's built-in policy pipeline; avoids a custom middleware; keeps `[RequirePermission]` declarative at the controller/action level; `PermissionPolicyProvider` falls back to `DefaultAuthorizationPolicyProvider` for non-permission policies so standard `[Authorize]` and role policies continue to function. | API authorization infrastructure | No |
| 2026-05-26 | Register `TestPermissionController` in `KnowledgeOps.Api.Tests` via `services.AddControllers().AddApplicationPart(typeof(AuthorizationApiTestFactory).Assembly)` — not as a production endpoint. | Allows integration-style authorization tests against real permission policies without adding business endpoints to the API or requiring a live database. The test-only controller is compiled into the test assembly only and never deployed. | Testing / API authorization | No |
| 2026-05-26 | Define `AsteriaOrgId` and `BorealOrgId` inline in `AuthorizationApiTestFactory` rather than importing `KnowledgeOps.Infrastructure.Persistence.SeedData`. | `KnowledgeOps.Api.Tests.csproj` does not reference the Infrastructure project and should not add that dependency. Inline GUIDs matching canonical seed values keep the test project dependency surface minimal. | Testing / project references | No |
| 2026-05-26 | `PermissionAuthorizationHandler` logs only `EventType`, `UserId`, and `OrganizationId` on permission denial; never logs permission names, tokens, or credentials. | Matches `docs/16-security-and-permissions.md` safe logging rules; prevents permission names from appearing in log aggregators or support exports where they could guide targeted attacks. | Authorization logging / security | No |
| 2026-05-26 | Future RAG authorization hook interfaces (`IRetrievalAuthorizationFilter`, `IPromptAuthorizationFilter`, `ICitationAuthorizationFilter`, `IDashboardAuthorizationFilter`) are compile-time contracts only — no implementation, no DI registration, no retrieval logic. | Issue #14 scope is permission catalog and enforcement infrastructure only; RAG workflows are deferred to Sprint 17+. Defining interfaces now establishes the authorization injection points and prevents future sprint from having to retrofit them. | Future RAG authorization / scope control | No |
| 2026-05-26 | `RoleVisibilityService` in the frontend is explicitly annotated as UX-only; each helper carries a comment stating backend enforces authorization independently. | Prevents future contributors from mistaking frontend visibility guards for authorization boundaries; aligns with `docs/16-security-and-permissions.md` Section 8.1 which requires backend to be the sole authorization boundary. | Frontend / authorization boundary | No |
| 2026-05-26 | `Chat.ViewInteraction` and `Chat.ViewCitations` are granted to all five MVP roles in the matrix, with a code comment documenting the Agent=own-only vs. Supervisor/KnowledgeAdmin/Manager/Admin=same-org-scoped convention; actual query-level enforcement is deferred to Sprint 17+. | The matrix must compile and tests must pass now; but the scoped-vs-own-only distinction requires chat workflow query predicates that do not exist until Sprint 17. Documenting the convention in the matrix prevents the deferred enforcement from being lost. | Role-permission matrix / chat | No |
| 2026-05-26 | Exclude `Documents.Enable`, `Documents.RetryProcessing`, and `KnowledgeGaps.*` from `AllMvpPermissions`. | These are Phase 2 permissions explicitly deferred in `docs/16-security-and-permissions.md`; including them before their owning sprints would cause false positives in Admin permission tests and confusion in the RolePermissionMatrix. | Permission catalog / scope | No |

| 2026-05-26 | Accept `X-Correlation-ID` only when it contains 1 to 100 ASCII letters, digits, `-`, or `_`; replace any other incoming value with a generated identifier. | The 100-character limit matches the existing audit correlation storage capacity and prevents unsafe caller-controlled correlation data from reaching error responses or audit records. | Operational safety / correlation IDs | No |
| 2026-05-26 | Reuse the existing `audit_log_entries` table through a best-effort `IAuditEventWriter` / `EfAuditEventWriter` baseline; emit only `UserLoginSuccess`, `UserLoginFailure`, `PermissionDenied`, and `HealthDetailsViewed` in Issue #15 and create no migration. | Sprint 8 needs safe operational traceability without failing user requests when audit persistence is unavailable or expanding persistence/schema scope. | Observability / audit persistence | No |
| 2026-05-26 | Expose public basic health and Admin-only detailed health limited to sanitized `application` and `database` statuses; a database-unavailable detail response is HTTP 503 without raw dependency information. | Only the database dependency is implemented in Sprint 8; storage, worker, retrieval, provider, and richer operational status remain in their owning future sprints. | Health API / scope control | No |

| 2026-05-26 | Place `UserManagementService` and `IUserManagementRepository` in `KnowledgeOps.Application/Users`; place `EfUserManagementRepository` in `KnowledgeOps.Infrastructure/Users`. | Maintains Clean Architecture: Application owns business rules (org-scope, self-lockout, final-Admin check, email normalization, password hashing, audit); Infrastructure owns EF Core persistence. No domain logic leaks to Infrastructure; no EF Core in Application. | Admin user management architecture | No |
| 2026-05-26 | Use `IUserAccessStateReader / EfUserAccessStateReader` for persisted-current-state authorization in `PermissionAuthorizationHandler`. For every protected request the handler re-queries the database for the actor's active status, current roles, and current organization — JWT role/status claims are not used for the permission check. | Stale-token issue: a disabled or demoted Admin holding an unexpired JWT must not continue authorizing protected operations. Re-querying the database on every request prevents stale-claim exploitation. Applies broadly to all permission-gated endpoints, not just user management. | Authorization / stale-token fix | No |
| 2026-05-26 | `Users.Disable` permission is checked at the Application layer (`UserManagementService`) via `IUserAccessStateReader` lookup in addition to the controller-level `Users.Update` gate. | Disabling a user is a higher-privilege action than general profile updates. The Application service re-reads persisted access state to ensure the actor still holds `Users.Disable` at the time of the operation, even within a single request. | User management security | No |
| 2026-05-26 | Use `IsolationLevel.Serializable` for all user-management write operations in `EfUserManagementRepository`. | Final-active-Admin check, email uniqueness, and idempotent role assignment require that the read (check) and write happen atomically. Serializable isolation prevents lost-update and phantom races under concurrent Admin operations. Acceptable overhead at MVP scale. | User management persistence | No |
| 2026-05-26 | Admin user-management endpoints are same-organization only: cross-organization user ID lookups return safe 404 without revealing existence. No multi-organization super-admin model. | ADR-010: Admin role is org-scoped in MVP. Leaking cross-org user existence would expose organizational membership. Same-org-only simplifies authorization and aligns with the scope boundary applied to all five MVP roles. | Admin scope / security | No |
| 2026-05-26 | `initialPassword` is request-only for MVP local-auth account bootstrap: it is hashed immediately via `IPasswordHasher` before any persistence call; it is never returned in any response, never logged, never audited, and never committed in any seed or fixture data. | Plaintext credential exposure risk. MVP has no invitation or email-verification workflow; an admin-provided initial password is the only practical way to bootstrap a new user account. The password is treated as ephemeral secret material and discarded after hashing. | Authentication / credential safety | No |
| 2026-05-26 | Self-lockout protections are enforced in `UserManagementService` (application layer), not only in the controller: (1) Actor cannot change own status away from Active; (2) Actor cannot remove own Admin role; (3) Final-active-Admin check prevents disabling or demoting the last active Admin in the organization. | Defense-in-depth: application-layer enforcement prevents bypass if a future caller skips the controller. Final-Admin check uses serializable-isolation repository read to prevent concurrent demotions. | Admin self-protection / security | No |
| 2026-05-26 | Frontend Admin UI uses `adminVisibilityGuard` (UX-only) to hide admin navigation for non-Admin sessions; backend `[RequirePermission]` attributes remain the sole authorization boundary. `adminVisibilityGuard` carries explicit code comments clarifying it is not a security control. | Consistent with existing `RoleVisibilityService` UX-only annotation pattern. Prevents future contributors from mistaking frontend guard for a security boundary. Backend remains authoritative for all `/api/v1/users` operations. | Frontend / authorization boundary | No |
| 2026-05-26 | No EF Core migration was created for Issue #16. Existing `users`, `user_roles`, `organizations`, and `audit_log_entries` tables are sufficient. `initialPassword` is never persisted; `password_hash` column already exists on `users`. | No schema gap was found during pre-implementation inspection. Adding a migration without a schema change would generate unnecessary noise and risk false diffs on future `dotnet ef migrations add` calls. | Persistence / migration scope | No |

| 2026-05-26 | `Document` stores canonical metadata and lifecycle timestamps and uses behavior-protected methods (`StartProcessing`, `MarkProcessed`, `MarkFailed`, `DisableRetrieval`, `IsEligibleForRetrieval`). `MarkFailed` requires a trimmed, non-blank reason no longer than 1000 characters. | Keeps the Sprint 10 domain aligned with the canonical record and persistence limit without introducing deferred processing or retrieval workflows. | Domain model design | No |
| 2026-05-26 | `Document.IsRetrievalEnabled` has a database default of `false`, and Issue #20 exposes no enable operation in Domain, Application, API, or frontend production code. | Newly recorded documents must not become retrieval-eligible implicitly; re-enable behavior remains out of scope. | Document lifecycle | No |
| 2026-05-26 | Retrieval disablement uses an atomic repository update that returns whether `true` changed to `false`; `DocumentService` emits `DocumentRetrievalDisabled` only when that returned transition is true. | Preserves idempotency and prevents duplicate audit events from repeated or competing disable requests. | Document service / audit | No |
| 2026-05-26 | `storage_location` column is NOT NULL with a test placeholder `"pending://document-metadata-only"`. | The column schema requires NOT NULL per `docs/14-database-design.md`; Sprint 10 does not implement file storage. Placeholder value makes the storage location explicit and traceable in test data. Real storage locations will be written by the Sprint 11 upload flow. | Document persistence / test data | No |
| 2026-05-26 | `DocumentsController` exposes list, detail, processing-status, and `POST /api/v1/documents/{documentId}/disable`. Upload is deferred to Sprint 11; enable and retry routes are deferred. | Implements only the canonical Sprint 10 surface while preserving deferred workflow boundaries. | API scope / documents | No |
| 2026-05-26 | Response DTOs (`DocumentResponse`, `DocumentProcessingStatusResponse`) do not expose `storage_location`. | `storage_location` is a storage-system internal reference; exposing it would leak infrastructure details and potentially pre-signed/internal blob paths to clients. | API security / information hiding | No |
| 2026-05-26 | `DocumentMetadataFoundation` includes canonical columns, `is_retrieval_enabled` default `false`, the four-state status constraint, six supporting indexes, and organization/user foreign keys; it was applied to disposable local SQL Server validation and exercised by SQL-gated tests. | The Issue #20 migration and repository persistence contract must be proven before PR preparation. | Migration / validation | No |

| 2026-05-26 | Place `IDocumentStorage` in `KnowledgeOps.Application`; place `LocalDocumentStorage` in `KnowledgeOps.Infrastructure`. | Application defines the storage contract (abstraction); Infrastructure owns file-system interaction and `IOptions` binding. Keeps the Application layer testable with fake storage without coupling to `System.IO` or `IOptions`. | Upload architecture / layer boundary | No |
| 2026-05-26 | Use `local://` URI scheme for stored document references (`StoredDocumentReference.Location = "local://{safeFileName}"`). | Decouples storage location representation from absolute paths; prevents absolute path disclosure; makes scheme-dispatch in `DeleteAsync` explicit and testable. | Document storage / information hiding | No |
| 2026-05-26 | Storage config key is `Storage:LocalDocumentsPath`; default development value is `.local/storage/documents`. | `Storage:` namespace avoids collision with other infrastructure settings; `.local/` is already gitignored; relative path is resolved to absolute via `Path.GetFullPath` at first use. | Upload configuration | No |
| 2026-05-26 | `UploadDocumentCommand` accepts `Stream` (not `IFormFile`). Application layer must not depend on ASP.NET Core types. | `IFormFile` is an ASP.NET Core abstraction; importing it into the Application layer would couple business logic to the web framework. The controller opens the stream and passes it to the command. | Layer boundary / dependency isolation | No |
| 2026-05-26 | Set `FormOptions.MultipartBodyLengthLimit` in `Program.cs` and `[RequestSizeLimit]` on the upload action in addition to Application-layer `MaxFileSizeBytes` validation. | Three-layer defense: Kestrel/IIS request size limit (infrastructure), model binding limit (framework), and business-rule size validation (application). Each layer catches oversized uploads at the earliest possible point. | Upload size limits | No |
| 2026-05-26 | Allow file extensions `.pdf`, `.txt`, `.md`, `.markdown`, `.docx` and their corresponding MIME types only; validate both extension and content type. | Defense-in-depth against extension/type mismatch: a PDF with `.exe` extension or an exe with `application/pdf` type both fail. Supported set is conservative and matches the roadmap document scope. | Upload validation | No |
| 2026-05-26 | `MakeSafeFileName` strips path traversal with `Path.GetFileName`, then replaces all characters except alphanumerics, `.`, `-`, and `_` with `_`. | Prevents path traversal in the storage root; produces safe filesystem names regardless of OS or user-supplied filename. | Upload security | No |
| 2026-05-26 | Atomic upload flow: validate → `storage.StoreAsync` → `repository.CreateAsync`. On `CreateAsync` failure, call `storage.DeleteAsync` (best-effort) before throwing `ApplicationServiceUnavailableException`. | Prevents orphaned files from accumulating in storage when database persistence fails. Best-effort cleanup is acceptable because storage failure during cleanup is logged and not re-thrown. | Upload atomicity | No |
| 2026-05-26 | `EfDocumentRepository.CreateAsync` relies on C# defaults for `Document.ProcessingStatus` (enum 0 = `Uploaded`) and `Document.IsRetrievalEnabled` (bool default = `false`). Explicit assignment is not possible because these properties have `private set;`. | `Document` entity uses `private set;` to protect lifecycle transitions; C# defaults happen to be correct. Comment in `CreateAsync` documents this intentional reliance on defaults. | Domain encapsulation / persistence | No |
| 2026-05-26 | `storageLocation` is not exposed in `DocumentResponse`, `DocumentProcessingStatusResponse`, or any Angular template or service model. | `storage_location` is an infrastructure-internal reference; exposing it would leak storage topology and `local://` scheme details to API consumers. | API security / information hiding | No |
| 2026-05-26 | `DocumentUploadAccepted`, `DocumentUploadRejected`, and `DocumentUploadFailed` are emitted as best-effort audit events. Rejected means validation failure; Accepted means the document was persisted; Failed means a storage or persistence error occurred. | Audit event names follow the existing operational pattern; three distinct types allow ops teams to distinguish validation failures from infrastructure failures without inspecting log details. | Audit event design | No |
| 2026-05-26 | Angular `DocumentUploadPage` uses `vi.spyOn(TestBed.inject(Router), 'navigate').mockResolvedValue(true)` in the `setup()` helper. The `Router` token is not overridden as a provider. | Overriding the `Router` DI token with a plain object breaks Angular's router factory (`root` is `undefined`), crashing all component tests in the spec. Spying on the real injected instance intercepts only the `navigate` call and leaves the router infrastructure intact. | Testing / Angular router | No |

| 2026-05-26 | Extract `AddJwtInfrastructure()` from `AddInfrastructure()` so the Worker host does not require `Jwt:SigningKey` or trigger `ValidateOnStart()` at startup. API host calls both; Worker calls only `AddInfrastructure()`. | `IOptions<JwtSettings>.ValidateOnStart()` fires at host startup; the Worker has no JWT responsibility and would fail to start if it tried to validate a signing key it does not need. | Infrastructure DI / Worker blocker | No |
| 2026-05-26 | `WorkerCorrelationContext` implements `ICorrelationContext` using `CorrelationIdPolicy.AcceptOrCreate(null)` and is registered as `Scoped` in the Worker host; `HttpCorrelationContext` (API-only) is not registered in the Worker. | The Worker has no `IHttpContextAccessor`; each processing scope gets a fresh correlation ID via the same existing policy that the API uses for missing/invalid headers. | Observability / Worker | No |
| 2026-05-26 | `WorkerSettings.PollingIntervalSeconds` defaults to 10 in code via `EffectivePollingIntervalSeconds`; `appsettings.json` includes an explicit `Worker:PollingIntervalSeconds` of 10. | A 0 or negative value is rejected by `EffectivePollingIntervalSeconds`; the default allows the Worker to start without any external configuration, matching the sprint requirement. | Worker configuration | No |
| 2026-05-26 | `DocumentProcessingOrchestrator.BuildSafeFailureReason` uses `ex.Message` only, trimmed and capped at 200 characters; never `ex.ToString()`. | Stack traces, absolute paths, and exception type names from `ex.ToString()` may expose infrastructure details or sensitive file paths to the audit log. `ex.Message` provides actionable context without leaking internals. | Processing safety / audit | No |
| 2026-05-26 | `ClaimForProcessingAsync` uses `ExecuteUpdateAsync` with a `WHERE ProcessingStatus == Uploaded && DeletedAt == null` guard to atomically transition to Processing. If `changedRows == 0`, the document was already claimed or deleted and the method returns null. | Prevents two concurrent Worker cycles from double-processing the same document. The atomic single-row update replaces any need for application-layer optimistic concurrency or distributed lock. | Processing atomicity | No |
| 2026-05-26 | `MarkProcessedAsync` and `MarkFailedAsync` both include a `WHERE ProcessingStatus == Processing` guard and do NOT modify `IsRetrievalEnabled`. | Placeholder processing must not silently enable retrieval; only explicit future enable operations (deferred to Phase 2) may do so. The guard prevents double-write or out-of-order transition races. | Processing lifecycle / retrieval safety | No |
| 2026-05-26 | `DocumentProcessingWorker` resolves `IDocumentProcessingOrchestrator` per-cycle from a fresh `IServiceScopeFactory` scope; the host-level `IDocumentProcessingOrchestrator` registration is `Scoped`. | EF Core DbContext is scoped; creating a new scope per cycle prevents long-lived context state accumulation and aligns with the recommended background-service scoped-service pattern. | Worker architecture | No |
| 2026-05-26 | Angular `DocumentDetailPage` polls `GET /api/v1/documents/{id}/processing-status` via `interval(5000)` with `takeUntil(destroy$)` and `switchMap`; polling stops automatically when a terminal status (`Processed` or `Failed`) is received, and `destroy$.next()` is called from `ngOnDestroy` for cleanup. | Non-terminal statuses (Uploaded, Processing) require client-visible progress updates that the server push from the Worker. The 5-second interval balances responsiveness against server load. `switchMap` cancels in-flight requests on each tick to prevent response pile-up. | Angular frontend / polling | No |
| 2026-05-26 | Angular polling tests use `vi.useFakeTimers({ toFake: ['setInterval', 'clearInterval'] })` (selective fake) instead of full `vi.useFakeTimers()` or Angular's `fakeAsync`. | Full timer faking (`vi.useFakeTimers()`) interfered with Angular's internal `setTimeout`-based async (HTTP request delivery, change detection scheduling), causing `HttpTestingController.verify()` to find 2 pending initial requests. Angular's `fakeAsync` requires Zone.js testing, which is not available in this Vitest setup. Selective faking of `setInterval`/`clearInterval` only intercepts RxJS `interval` without breaking Angular's internals. | Testing / Angular polling | No |

| 2026-05-27 | Add `string StorageLocation` to `ManagedDocument` at position 7 (after `FileSizeBytes`); never serialize it to API DTOs or Angular models. | The processing step must open the file via `IDocumentStorage.OpenReadAsync`; `ManagedDocument` is the only context object passed to `IDocumentProcessingStep.ExecuteAsync`. `StorageLocation` must never reach API JSON responses or Angular service interfaces — the existing `ResponseDoesNotExposeStorageLocationOrInternalFields` test guards this. | Document processing / information hiding | No |
| 2026-05-27 | Add `OpenReadAsync(string storageReference, CancellationToken)` to `IDocumentStorage`; implement in `LocalDocumentStorage` using the same path-containment guard as `DeleteAsync` (root-with-separator prefix check). Throw `InvalidOperationException` with a generic safe message on scheme mismatch or path-traversal attempt — never log the resolved absolute path. | The processing step must read the stored file without bypassing the containment invariant that `DeleteAsync` already enforces. Path details must not appear in exception messages or structured log fields. | Document storage / security | No |
| 2026-05-27 | `IDocumentTextExtractor` is defined in Application; `TxtMarkdownTextExtractor` (Infrastructure) supports `text/plain` and `text/markdown` including parameterized forms (e.g. `text/plain; charset=utf-8`). PDF, DOCX, and other types are not supported — unsupported types throw `DocumentExtractionException("Unsupported document format for text extraction.")`. | Only plaintext formats can be read character-by-character without a binary parser. Failing safely on unsupported types produces a controlled, non-secret failure reason that the orchestrator stores verbatim via `BuildSafeFailureReason`. | Text extraction / scope | No |
| 2026-05-27 | `TxtMarkdownTextExtractor.ExtractAsync` normalizes all line endings to `\n` (replaces `\r\n` then `\r`). I/O errors are wrapped in `DocumentExtractionException("Document text extraction failed.")` — the original exception is discarded to prevent internal path or encoding details from reaching the failure-reason column. | Consistent line endings simplify downstream chunking boundary logic and prevent chunk-boundary variance between Windows-authored and Unix-authored documents. Error wrapping follows the controlled safe-message pattern required by `docs/22-implementation-guardrails.md`. | Text extraction / security | No |
| 2026-05-27 | `IDocumentChunker` / `DocumentChunker` (Infrastructure) uses a sliding-window algorithm: `MaxChunkCharacters = 1200`, `OverlapCharacters = 150`. `token_estimate = (int)Math.Ceiling(characterLength / 4.0)`. Empty text throws `DocumentChunkingException("No usable text could be extracted from the document.")`. Internal errors throw `DocumentChunkingException("Document chunking failed.")`. | Sliding-window overlap preserves sentence/paragraph context across chunk boundaries for future retrieval. Constants are hard-coded per the closed decision; no configuration surface is exposed. Token estimate uses the commonly accepted heuristic of 4 characters per token. | Chunking algorithm | No |
| 2026-05-27 | `IDocumentProcessingTransactionFactory` / `IDocumentProcessingTransaction` are defined in Application; `EfDocumentProcessingTransactionFactory` / inner `EfDocumentProcessingTransaction` are in Infrastructure. The orchestrator wraps `processingStep.ExecuteAsync` + `MarkProcessedAsync` in a transaction; on any exception from either, `RollbackAsync` is called before re-throwing. | Application must not reference EF Core; the factory abstraction keeps the transaction boundary in Application-layer orchestration code without leaking `IDbContextTransaction`. Wrapping both chunk persistence (inside the step) and `MarkProcessedAsync` in the same transaction ensures the document status and its chunks are always committed together or rolled back together. | Transaction atomicity / layer boundary | No |
| 2026-05-27 | `ExtractAndChunkDocumentProcessingStep` validates supported content type before opening the file; throws `DocumentExtractionException("Unsupported document format for text extraction.")` for unsupported types without reading any bytes. Empty post-extraction text throws `DocumentExtractionException("No usable text could be extracted from the document.")`. Zero chunks (post-chunking) throws `DocumentChunkingException("No usable text could be extracted from the document.")`. | Early validation prevents unnecessary file I/O for PDF/DOCX documents. The controlled exception messages flow through `BuildSafeFailureReason` unchanged and are stored in `FailureReason` — they are designed to be user-presentable without leaking internal state. | Processing step design | No |
| 2026-05-27 | `PlaceholderDocumentProcessingStep` deleted; `ExtractAndChunkDocumentProcessingStep` registered in its place in `Application.DependencyInjection`. The two placeholder-specific orchestrator unit tests (`ProcessNextAsync_DoesNotReadFileContent`, `ProcessNextAsync_DoesNotCallAIProvider`) were removed as they tested placeholder no-I/O behavior no longer applicable. Orchestrator tests were updated to use `NoopProcessingStep` and `NoopTransactionFactory` fakes. | Sprint 12 placeholder is no longer appropriate once real extraction/chunking exists. Retaining placeholder-contract tests after replacing the step would create misleading assertions about the production step's I/O behavior. | Codebase hygiene / test maintenance | No |
| 2026-05-27 | `InternalsVisibleTo("KnowledgeOps.Application.Tests")` added to `KnowledgeOps.Application.csproj`; `InternalsVisibleTo("KnowledgeOps.IntegrationTests")` added to `KnowledgeOps.Infrastructure.csproj`. | `ExtractAndChunkDocumentProcessingStep` (Application) and `DocumentChunker`/`TxtMarkdownTextExtractor` (Infrastructure) are `internal sealed`; the respective test projects need access to construct them directly. No separate Infrastructure.Tests project exists; unit tests for Infrastructure algorithmic implementations are co-located in IntegrationTests (where Infrastructure is already referenced). | Testing / access control | No |

| 2026-05-27 | Sprint 14 embedding statuses are `Ready` and `Failed` only; if the provider returns an invalid or empty vector, the record is written with `status = Failed` and a safe failure reason `"Embedding vector was invalid."` No `Generated`, `Invalid`, or `Pending` status is introduced in Sprint 14. | `docs/14-database-design.md` Section 13.5 lists Pending/Processing/Ready/Failed as canonical options; the audit found `Pending` and `Processing` are transient states not needed for a synchronous fake provider. Using only terminal statuses (Ready/Failed) avoids a partial-write state that would require additional cleanup logic and simplifies the retrieval-eligibility predicate. | Embedding domain model / EmbeddingStatus enum | No |
| 2026-05-27 | `DocumentProcessingOrchestrator` accepts `IEnumerable<IDocumentProcessingStep>` (materialized to `IReadOnlyList<IDocumentProcessingStep>` via `.ToList()` in the primary constructor). Steps execute in registration order: (1) `ExtractAndChunkDocumentProcessingStep`, (2) `GenerateChunkEmbeddingsProcessingStep`. The document is marked `Processed` only after all steps succeed; if any step throws, the transaction is rolled back and the document is marked `Failed`. | Issue #27 adds a second processing step; a single-step constructor field cannot grow without a breaking change. Materializing to list at construction time prevents deferred-enumeration issues. Ordered execution via registration order in `Application.DependencyInjection` is the simplest DI-native approach and avoids a custom step-ordering attribute. | Processing pipeline / orchestrator | No |

| 2026-05-27 | Sprint 15 uses a local SQL-backed retrieval adapter over `chunk_embeddings.vector_data` JSON. | Issue #28 requires an MVP retrieval/vector abstraction without Azure AI Search, external vector services, network dependency, live credentials, or provider SDKs. SQL-backed cosine search keeps CI deterministic while preserving future replaceability. | Retrieval / Infrastructure adapter | No |
| 2026-05-27 | Add `AddChunkEmbeddingIndexMetadata` migration with nullable `index_status`, `indexed_at`, and `index_failure_reason` on `chunk_embeddings`; index statuses are terminal `Indexed` / `Failed` only. | Sprint 15 needs retrieval-search eligibility separate from embedding generation status. Nullable metadata keeps existing rows valid, and terminal statuses avoid pretending an asynchronous indexing queue exists. | Retrieval indexing / database metadata | No |
| 2026-05-27 | `SemanticQueryRequest` uses `QueryVector`, not `QueryText`; `ISemanticSearchProvider` does not call `IEmbeddingProvider`. | Query text to query embedding orchestration belongs to a later RAG/chat sprint. Keeping semantic search vector-only prevents premature prompt/chat workflow coupling. | Application retrieval contract | No |
| 2026-05-27 | Semantic retrieval filters `organization_id` and document/chunk eligibility in SQL before loading vectors and before scoring. | ADR-010 and BR-015 require organization isolation. Filtering before vector parsing/scoring prevents cross-organization candidates from affecting ranking or malformed-vector handling. | Retrieval security / organization scope | No |
| 2026-05-27 | Sprint 16 adds an Application-level eligible semantic retrieval service only; no API endpoint, chat endpoint, frontend route, prompt construction, answer generation, or citation mapping is introduced. | Issue #29 is the retrieval safety gate before future RAG. Keeping it Application-only allows tests to prove orchestration and eligibility without exposing a user-facing retrieval surface prematurely. | Retrieval orchestration / scope control | No |
| 2026-05-27 | Eligible retrieval derives organization scope from persisted active `UserAccessState`, not request input or JWT claims alone. | ADR-010 requires current organization scope to be authoritative. Revalidating active persisted state prevents disabled/demoted users or stale tokens from driving retrieval. | Retrieval security / authorization | No |
| 2026-05-27 | Query text enters only `EligibleSemanticRetrievalService`; semantic search remains QueryVector-only. | The Application use case owns query embedding orchestration for Sprint 16. `ISemanticSearchProvider` stays isolated from raw question text and from `IEmbeddingProvider`. | Retrieval provider boundary | No |
| 2026-05-27 | Add `IRetrievalEligibilityRepository` for bulk Application-level candidate revalidation after semantic search. | Sprint 15 provider filtering is necessary but not sufficient. Sprint 16 adds defense-in-depth against stale, disabled, deleted, unindexed, failed, or cross-scope candidates before any future prompt can use them. | Retrieval eligibility / data access | No |
| 2026-05-27 | Defer physical `retrieval_results` / `retrieval_queries` persistence until a future chat interaction sprint; return `RetrievalQueryId` and SHA-256 `QueryHash` as the Sprint 16 contract. | Canonical `retrieval_results` belongs to stored `chat_interaction_id`. Creating a table before `chat_interactions` would either violate the canonical schema or force premature nullable relationships. | Retrieval persistence / database scope | No |
| 2026-05-27 | Eligible retrieval results and audit messages exclude raw query text, chunk/document text, vectors, prompts, and generated answer content. | Retrieval metadata is enough for traceability in Sprint 16; protected content and future prompt/answer material must not leak through logs or interim result contracts. | Retrieval safety / observability | No |
| 2026-05-30 | Add `CitationId` to the Application `CitationResponse` and map Issue #40 API answer states with an explicit switch instead of enum serialization. | The Sprint 20 transport contract requires stable citation identifiers and public answer-state names (`GroundedAnswer`, `InsufficientContext`, `ProviderFailure`) that differ from internal enum names. | Chat API / citation contract | No |

| 2026-05-30 | `KnowledgeAdmin` is own-only for Sprint 21 chat history, interaction detail, and citation access. Scoped review is limited to `Supervisor`, `Manager`, and `Admin` via `Chat.ViewScopedHistory`. | Audit finding: `docs/16-security-and-permissions.md` Section 7.3 explicitly names only Supervisor/Manager/Admin for scoped session/interaction access. KnowledgeAdmin lacks `Chat.ViewScopedHistory` in the matrix. | Chat history authorization / Sprint 21 | No |
| 2026-05-30 | Add `chat_sessions.status` with default `Active` because the canonical `ChatSessionResponse` in API design Section 7.12 requires a session status field. Domain entity gained `Status` property with `StatusActive = "Active"` constant. | Missing column identified in pre-implementation audit. Canonical API contract requires the field. | Database schema / domain model | No |
| 2026-05-30 | Add `FK_citations_documents_document_id` FK constraint from `citations.document_id` to `documents.document_id` in the `ChatSessionStatusAndCitationDocumentFk` migration. | Pre-implementation audit found the citations table lacked referential integrity on `document_id`. The column and index already existed; only the FK constraint was missing. | Database integrity | No |
| 2026-05-30 | Do not add a separate `insufficient_context` bit column in Sprint 21. Derive `insufficientContext` from `AnswerState == InsufficientContext` in the response DTO mapping. | Adding the column without schema evidence is premature; the existing `answer_state` int column captures InsufficientContext correctly. API responses carry `insufficientContext: bool` derived at mapping time. | Database / API contract | No |
| 2026-05-30 | `GET /api/v1/chat/sessions` returns own sessions by default; organization-scoped listing is explicit via `?scoped=true` query parameter and available only to `Supervisor`, `Manager`, and `Admin`. `ChatHistoryService.GetSessionsAsync(scopedReview)` delegates `Chat.ViewScopedHistory` enforcement to the service layer. | Keeps own-history as the safe default; scoped review is an explicit opt-in requiring the appropriate permission. | Chat history endpoint | No |
| 2026-05-30 | `POST /api/v1/chat/sessions` creates a simple active session without AI, retrieval, prompt construction, citations, or answer generation. Session title is optional. New sessions default to `Active`. | Sprint 21 scope; explicit session creation is needed for Angular UX when the user wants a named session before asking a question. | Chat session creation | No |
| 2026-05-30 | Historical citations remain visible to authorized users even if the source document is later retrieval-disabled. Citation records stored in `citations` table are independent of `documents.is_retrieval_enabled`. | Canonical database design Section 11.4 allows historical citations for review. Decoupling display from retrieval eligibility preserves evidence integrity. | Citation / history display | No |
| 2026-05-30 | `ChatHistoryService` uses `IUserAccessStateReader` to re-read persisted access state (not JWT claims) for every chat history operation. Organization scope is authoritative from `UserAccessState.OrganizationId`. Cross-org access returns null (safe 404). | Consistent with the established persisted-state authorization pattern from Sprint 7 / Issue #14. Prevents stale-token exploitation of history endpoints. | Chat history authorization | No |

| 2026-06-01 | D2: Issue #43 uses [RequirePermission] + IUserAccessStateReader + org-scoped repository queries. IDashboardAuthorizationFilter not implemented. | Decision D2 approved prior to implementation. The hook interface exists as a compile-time contract only (no implementation, no DI registration). Application-layer org scope comes from persisted UserAccessState, not client input. | Dashboard authorization | No |
| 2026-06-01 | D3: Dashboard endpoints support optional ?from= and ?to= query parameters. Default period is last 30 days. If only from provided: [from, now]. If only to provided: [to-30d, to]. Validated UTC. | Decision D3 approved prior to implementation. Implemented in DashboardDateRange.Create(from, to) factory. | Dashboard query params | No |
| 2026-06-01 | D5: activeUsers = COUNT(DISTINCT user_id) from chat_interactions within authenticated user's org and selected period. | Decision D5 approved prior to implementation. Implemented in EfDashboardRepository using .Select(i => i.UserId).Distinct().CountAsync(). | Dashboard active users metric | No |
| 2026-06-01 | D8: dashboardVisibilityGuard added as UX-only Angular route guard for /dashboard route. Backend authorization ([RequirePermission]) is the security source of truth. | Decision D8 approved prior to implementation. Guard is explicitly annotated as UX navigation guidance only, not a security boundary. | Angular frontend guard | No |
| 2026-06-01 | D1: Issue #44 audit-log endpoint uses a minimal flat response and supports only `from`, `to`, and `eventType` filters. | Keeps the MVP supportability surface safe and predictable. No advanced search, export, raw payload viewer, or cross-organization audit view is introduced. | Audit supportability API | No |
| 2026-06-01 | D3: Issue #44 includes minimal Angular admin/support views for processing failures and audit log review. | The roadmap calls for supportability completion, and the views expose only safe metadata with loading, error, and empty states. Backend authorization remains authoritative. | Angular admin/support UX | No |
| 2026-06-01 | D6: Issue #44 does not retrofit `OrganizationScopeFailure`; `PermissionDenied` remains the persisted controller-layer authorization failure and application-layer failures stay safe exceptions/logging. | Avoids changing established observability semantics while adding read-only support endpoints. The new endpoints still enforce persisted-current-user org scope. | Observability / authorization events | No |
| 2026-06-01 | D8/D11: Existing event names remain canonical: `DocumentProcessingSucceeded`, `EligibleSemanticRetrievalCompleted`, `EligibleSemanticRetrievalFailed`, `ChatAnswerGenerationCompleted`, `ChatAnswerGenerationFailed`, `UserRoleAssigned`, and `UserRoleRemoved`. | Issue #44 extends the catalog only with `AuditLogViewed`; it does not rename or normalize prior event strings. | Audit event catalog | No |

| 2026-06-01 | G-1: `EfChatInteractionRepository.FindByIdAsync` now requires `organizationId` as a parameter and filters by both `id` AND `organizationId` at the DB level. `IChatInteractionRepository` contract updated; `ChatHistoryService` passes `activeState.OrganizationId`; all fake/mock implementations in Application and API test assemblies updated. The redundant application-layer `interaction.OrganizationId != activeState.OrganizationId` check was removed because it is now enforced at the DB level. No migration required; no behavior change for authorized access. | Defense-in-depth: consistent with `IDocumentRepository.FindAsync(documentId, organizationId)` and other org-scoped repository methods already in use. Prevents cross-org disclosure even if a future caller skips the application-layer service check. | Chat history / repository security | No |
| 2026-06-01 | G-2 through G-7: Cross-org API-layer tests added to `DashboardControllerTests`, `FeedbackControllerTests`, `ChatHistoryControllerTests`, and `DocumentsControllerTests`. Tests verify HTTP 404 for cross-org resource access, that service/controller receives OrgA's persisted org ID not OrgB's, and that response bodies do not contain OrgB org IDs or prohibited content fields (questionText, answerText, storageLocation, etc.). | Release-blocking security hardening: proves the MVP cannot disclose unauthorized data through protected APIs across organization boundaries. | Cross-org security tests / Issue #45 | No |
| 2026-06-01 | No authentication model, RBAC roles, permissions, EF Core migrations, RAG pipeline, retrieval behavior, prompt construction, citation generation, or AI provider behavior was changed in Issue #45. | Issue #45 scope is defense-in-depth fix + cross-org tests only. All security boundaries remain intact. | Scope confirmation / Issue #45 | No |
| 2026-06-01 | Issue #46 uses xUnit + `WebApplicationFactory<Program>` for in-process E2E smoke tests; Playwright/Cypress/browser E2E and CI workflow changes remain out of scope. | Approved Sprint 26 direction called for deterministic local/CI-suitable smoke coverage without adding a browser harness or workflow churn. | E2E smoke testing | No |
| 2026-06-01 | Issue #46 E2E uses deterministic fake providers/services and no live AI calls, production credentials, or real data. | MVP test completion must validate API/auth/safe-response behavior without network or provider nondeterminism. | AI/provider isolation | No |
| 2026-06-01 | Issue #46 adds SQL-gated repository coverage but does not claim SQL pass when `ConnectionStrings__DefaultConnection` is absent. | Existing integration convention skips SQL tests without a configured SQL Server; reporting must distinguish compiled/skipped from actually executed. | SQL-gated validation | No |

| 2026-06-01 | Issue #47: Option B CI strategy — standard `ci.yml` runs on PR/push/dispatch with non-SQL backend tests, Angular build+tests, and Docker build validation; SQL integration tests are in a separate `integration-tests.yml` triggered only by `workflow_dispatch`. | Prevents CI from blocking on unavailable SQL Server connection; SQL-gated tests require `SQL_SA_PASSWORD` repository secret and are intentionally opt-in. | CI strategy | No |
| 2026-06-01 | Issue #47: Multi-stage Dockerfiles created for API (`mcr.microsoft.com/dotnet/aspnet:10.0` runtime), Worker (`mcr.microsoft.com/dotnet/runtime:10.0` runtime), and Frontend (`node:24-alpine` build, `nginx:alpine` runtime). Build context is repository root for .NET; `./frontend` for Angular. Tests directory is NOT copied into Dockerfiles — only `src/` is needed for API and Worker publish. | Keeps images minimal; no secrets or credentials baked in; runtime-only image excludes SDK and test binaries. | Container configuration | No |
| 2026-06-01 | Issue #47: `*.local.json` added to `.gitignore`. | Matches `docs/18-deployment-and-devops.md` Section 11.2 recommended gitignore entries for local JSON secrets; prevents `settings.local.json` and `appsettings.Local.json` from being committed accidentally. | Security / Environment config | No |
| 2026-06-01 | Issue #47: `dotnet build` used in CI workflows (not `dotnet msbuild`). | `dotnet build` is the standard CLI command and is appropriate for Linux GitHub Actions runners; `dotnet msbuild` with `-t:Build` was used locally as a workaround for VS Code background compiler lock conflicts (see `feedback-build-workaround.md`). CI runners do not have this lock conflict. | CI build command | No |
| 2026-06-01 | Issue #47: Node 24 + `corepack enable` used for frontend CI. | Matches `frontend/package.json` `"packageManager": "npm@11.12.1"`; `corepack enable` ensures the correct npm version is used without a separate install step. | Frontend CI toolchain | No |
| 2026-06-01 | Issue #47: Angular build output path confirmed as `dist/frontend/browser/` for project named `frontend` using `@angular/build:application` (Angular 17+). | Verified by running `npm run build` locally and confirming `dist/frontend/browser/` is the output directory. The Frontend Dockerfile copies from `/app/dist/frontend/browser`. | Frontend Dockerfile / Angular output | No |

## Update Rule

Add a concise entry when a future issue makes a material implementation choice. If a choice changes an accepted architecture decision, identify the required ADR action rather than treating this log as approval.
